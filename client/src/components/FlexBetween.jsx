import styled from "@emotion/styled";
import { Box } from "@mui/material";


const FlexedBetween = styled(Box)({
    display:'flex',
    justifyContent:'space-between',
    alignItems:'center'
})

export default FlexedBetween